# ME660

ME 660 - Nonlinear Finite Element Methods

## Prerequisites

* cmake
  ```sh
  sudo apt install cmake
  ```
* Deal.II
  
## Installation

Build the project
```sh
mkdir build && cd build
cmake ../.
make
```